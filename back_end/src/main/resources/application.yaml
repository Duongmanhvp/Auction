spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/auction
    username: root
    password: Vinhdangcap03@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
  mail :
    host : smtp.gmail.com
    port : 587
    username : dinhmanhbh03@gmail.com
    password : hchj rjca tpzg kwpn
    properties:
      mail:
        smtp:
          auth : true
          starttls:
            enable : true
  data:
    redis:
      host: localhost
      port: 6379
      ssl:
        enabled: true

server:
  port: 8080
  servlet:
    context-path: /auction